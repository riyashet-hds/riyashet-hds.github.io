
# Unused legacy stylesheet from the old hand-coded site
styles.css

# Project source notebooks and READMEs live in each project's own repo, not the
# published site. Keep them locally for reference, but never push them.
assets/projects/**/*.ipynb
assets/projects/**/README.md
